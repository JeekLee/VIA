// Client-side API (can be used in client components)
export * from './memberClient';

// Note: Server-only APIs should be imported directly:
// - import { getMyMemberInfo } from '@/entities/member/api/member'
// - import { getCurrentMember } from '@/entities/member/api/getCurrentMember'
